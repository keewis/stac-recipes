- id: "ODYSSEA-IFR-L3S-GLO-GIR_010-v3"
  description: >-
    For the Global Ocean- Sea Surface Temperature L3 Observations. This product
    provides daily foundation sea surface temperature from multiple satellite
    sources. The data are intercalibrated. This product consists in a fusion of
    sea surface temperature observations from multiple satellite sensors, daily,
    over a 0.1° resolution global grid. It includes observations by polar
    orbiting (NOAA-18 & NOAAA-19/AVHRR, METOP-A/AVHRR, ENVISAT/AATSR,
    AQUA/AMSRE, TRMM/TMI) and geostationary (MSG/SEVIRI, GOES-11) satellites .
    The observations of each sensor are intercalibrated prior to merging using a
    bias correction based on a multi-sensor median reference correcting the
    large-scale cross-sensor biases.3 more datasets are available that only
    contain "per sensor type" data: Polar InfraRed (PIR), Polar MicroWave (PMW),
    Geostationary InfraRed (GIR)
  extent:
    # spatial extent as terms. Possible values:
    # - bbox: bbox(min_x, min_y, max_x, max_y)
    # - global: alias of bbox(-180, -90, 180, 90)
    spatial: "bbox(-180, -80, 180, 80)"
    # date range as "/" separated string
    temporal: "1982-01-01T00:00:00Z/2022-12-31T00:00:00Z"
  # use a `>-` block if the title exceeds the line length
  title: >-
    Global High Resolution ODYSSEA Sea Surface Temperature
    Multi-sensor L3 Observations
  # optionally (one url per line):
  stac_extensions:
    - "https://stac-extensions.github.io/datacube/v2.2.0/schema.json"
  license: "proprietary"
  keywords:
    - "Oceans"
    - "Ocean Temperature"
    - "Sea Surface Temperature"
  providers:
    - name: "CMEMS"
      roles: ["host"]
      url: "https://marine.copernicus.eu"
    - name: "Ifremer"
      roles:
        - "producer"
      url: "https://doi.org/10.48670/mds-00329"
  links:
    - rel: "license"
      href: "https://marine.copernicus.eu/user-corner/service-commitments-and-licence"
      title: "Legal notice of the CMEMS service"
    - rel: "cite-as"
      href: "https://doi.org/10.48670/mds-00329"
      title: "Citeable DOI"
  # optionally, assets:
  assets:
    # TODO: add link to thumbnail / additional resources
    {}
